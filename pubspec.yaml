name: falconet
description: Modular DI, routing, and HTTP for Flutter.
publish_to: 'none'

version: 1.0.0

environment:
  sdk: ^3.12.2

dependencies:
  dio: ^5.10.0
  flutter:
    sdk: flutter
  get_it: ^9.2.1
  go_router: ^18.0.1
  material_ui: ^1.2.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  mocktail: ^1.0.5
  very_good_analysis: ^10.3.0
